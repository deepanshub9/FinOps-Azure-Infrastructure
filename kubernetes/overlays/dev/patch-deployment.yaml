apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-cost-advisor
  namespace: dev
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: app
          resources:
            requests:
              cpu: 75m
              memory: 96Mi
            limits:
              cpu: 200m
              memory: 192Mi
